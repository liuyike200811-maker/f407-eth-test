# 工作流规则

## 强制：每次改动后必须本地提交

对本仓库中任何文件的修改（代码、配置、文档等）——无论是本次会话内完成的一次改动，还是一系列相关改动完成后——都必须执行一次 **本地 git commit**。

- 提交后**必须 `git push` 到 origin/main**（用户已于2026-08明确要求"每次改动都必须push"，覆盖此前"只本地提交"的默认规则）。
- **每次提交并push后必须在对话回复中明确说明**：已提交、commit 号、已push、改了哪些文件/内容。不允许"改了但没说"。
- 提交信息使用中文，简明描述改了什么、为什么改。
- 不要跳过 hooks（不要用 `--no-verify`）。
- 如果改动是探索性/临时的还未确认，可以先完成并验证后再提交，但确认后不要忘记提交，也不要让多次不相关的改动堆积在一次提交里。
- 提交前正常执行安全检查：`git status`/`git diff` 确认没有意外包含的密钥或不该提交的文件。

## 强制：不确定的选型/参数，先问再动手

遇到硬件型号、参数等**不确定/未经用户确认**的选择点（例如"这几种芯片型号选哪个"），
**不要自行猜一个就直接写代码实现**——哪怕代码里写清楚"这是猜的、待验证、可切换"也不行。
必须先提出选项、等用户确认或明确授权"你自己选一个试"之后，才能动手改代码。
（2026-08 用户明确指出：LCD面板型号(HX8357D/ILI9481/ILI9488/ST7793四选一)未确定就
直接实现了ILI9488分支，属于违规操作。）

# STM32 踝关节康复运动控制 —— 代码导读

给自己以后回看用。核心就一个文件 `App/ecat_motion.c`，其余是支撑。

## 一、先建立整体印象（一句话）

板子 = EtherCAT 主站（SOEM）+ 三轴 CSV 速度控制 + 串口/HMI 命令。
开机 → 扫从站 → 配 PDO → 进 OP → 使能 → **待机听命令** → 收到命令跑一段运动 → 回待机。

对照参考：Linux 原型 `../../test_xinje_csv_wave_v2.c`，控制算法和板子**逐字一致**，
想理解某段逻辑时可以两边对照（PC 版更好读，没有裸机那些初始化噪声）。

## 二、推荐阅读顺序（自顶向下，别从头一行行读）

### 1. 主骨架：`ecat_motion_run()`（文件最下面）
整个程序的入口和流程。从上到下就是开机全过程：
`uart_log_init → modbus_init → ecx_init(网卡) → ecx_config_init(扫从站)
→ PRE-OP 配 PDO(setup_pdo_csv) → SAFE-OP → OP → CiA402 使能(0x06→0x07→0x0F)
→ 进入"待机 while 循环"`。
**读懂这个函数 = 读懂程序骨架。** 里面的 `g_ec_phase` 是阶段编号，出错时能定位卡在哪。

### 2. 心跳节拍：`cycle()`
**全程最关键的一个函数。** 每调一次 = 一个 4ms 通信周期：发过程数据 → 收 → 睡到
下一个 4ms 绝对时刻。所有运动循环都靠不停调 `cycle()` 往前推。
- 为什么用"绝对时刻"睡而不是相对延时：注释里写了，是为了周期严格 4ms 不漂，
  否则闭环里 `cmd_pos += v*DT` 的积分会和伺服实际走的对不上 → 发飘。
- ⚠ 排查运动"混乱"时踩过坑：真正的元凶是**运动循环里的阻塞打印**，不是这里。
  见 `~/.claude` 记忆 `motion-jitter-uart-blocking`。

### 3. 命令怎么进来：`poll_cmd()` + `feed_cmd_byte()` + `handle_cmd()`
三路命令并收：USART1(CH340)、USB 虚拟串口、Modbus(HMI)。攒够一行(\r/\n)就解析，
命令只改全局标志（`g_run_request`/`g_do_home`/`g_abort`/`g_quit` 和各参数），
**不直接执行**。真正执行在待机循环里根据这些标志调运动函数。
命令表：`?`帮助 `h`归零 `g<0-4>`跑模式 `x`急停 `q`下电 `a/b/f/w/r/c`改参数。

### 4. 一次运动长什么样：`run_rehab_mode(mode)`
一个模式的完整流程 = **上升 → 运动 → 下降 → 回原点**：
- 上升/下降用 `move_ramp()`（加速-匀速-减速三段）。
- 中间"运动"段分两类：
  - `mode 0` 综合波浪：电缸空间三轴差 120° 相位的正弦。
  - `mode 1~4`：先在**踝角空间**给轨迹(`get_angles`)，再用**正运动学**(`fk_3rps`)
    换算成三个电缸长度，差分得每轴速度。
- 每帧都 `cycle(); poll_cmd();`，所以运动中也能即时急停/报警。

### 5. 闭环怎么算：`vel_closed()` + `limit_out()`
这是"软件位置闭环"的核心。每轴：
`下发速度 = 前馈速度 + KP×(期望位置 − 实际位置)`，纠正量再过个低通、限死区。
- `cmd_pos` = 前馈速度的积分 = 理想轨迹位置。
- `fb_sign` = 反馈方向自锁：开头一段纯前馈(开环)运动，用自然位移判定编码器极性，
  锁定后才闭环，防止接反跑飞。
- `limit_out` = 限速 + 限加速度，防突跳。
- 增益常量在文件顶部：`KP_POS / VMAX / AMAX / LPF_TAU / DEADBAND`（和 PC 版一致）。

### 6. 运动学：`fk_3rps()` / `get_angles()`
`get_angles` 按模式给出当前相位的踝角 (α内翻外翻, β跖背)；
`fk_3rps` 是 3-RPS 并联机构正解：踝角 + 工作高度 → 三根电缸长度。纯数学，看注释即可。

### 6.5 传感器实时跟随：`run_sensor_mode()`
新增模式（串口发 `s` 进入）。结构和 `run_rehab_mode` 平级：**上升→零偏标定→实时跟随→归平下降**。
和预设模式的唯一区别：目标踝角不是参数化正弦，而是**读 WT901 传感器**（`sensor_get()`）。
- 传感器 Roll→α、Pitch→β，减零偏 → EMA平滑 → 死区 → 限幅 → `fk_3rps` 逆算杆长 →
  相邻帧长度差分得前馈速度 → 还是走 `vel_closed()` 闭环。
- 安全：标定期没数据自动退出；跟随中信号丢失>300ms 冻结保持；发 `x` 退出会先归平再下降。
- 传感器驱动在 `App/sensor_wt.c`（USART6/PC7 收帧）。链路约定见项目根
  `传感器接入-STM32端对接契约.md`（跟 Windows 转发端对齐用）。

### 7. 其余支线（用到再看）
- `run_homing()`：归零，靠"堵转检测"（速度掉到阈值下持续若干帧=撞限位）。
- `setup_pdo_csv()`：给每个伺服配 CSV 模式的 PDO 映射（SDO 写 0x1600/0x1A00）。
- `graceful_shutdown()`：下电脱力、退回 INIT，之后可安全断电/拔网线。
- `modbus_idle_loop()`：没扫到从站时的降级空转，仍服务 Modbus（单测用）。
- `modbus_sync()` / `modbus_write_feedback()`：HMI 那套，把 Modbus 寄存器 ↔ 本地状态。

## 三、关键全局变量速查
- `g_run_request/g_do_home/g_abort/g_quit`：命令标志（命令线程置位，主循环消费）。
- `g_status`：运行状态字（0启动 1待机 2运行 3归零 4故障 5下电），HMI 反馈用。
- `g_wkc`：EtherCAT 工作计数器，==从站数×3(=9) 才算全通。
- `cmd_pos/v_last/corr_filt/fb_sign[轴]`：闭环每轴状态。
- 顶部 `#define`：机械常量(导程/脉冲数)、闭环增益、运动默认参数(可被命令/HMI 改)。

## 四、其余文件
- `App/sensor_wt.c/.h`：WT901 姿态传感器接入（USART6/PC7 收帧，帧解析+校验）。传感器跟随模式用。
- `App/modbus_slave.c/.h`：Modbus RTU 从站协议引擎（USART3, 站号1）。HMI 联调用。
  里面 `modbus_loopback_selftest_tx()` 是回环自测件，平时空操作，HMI 排查时才开
  （`#define MB_LOOPBACK_TEST`）——**这是唯一保留的调试件**，因为 HMI 还没通。
- `App/uart_log.c`：日志输出（USART1→CH340→USB，同时镜像到 USB CDC）。
  ⚠ 它是**阻塞式**发送（每字符忙等），所以**运动循环里绝不能调它**（会撑破 4ms）。
- `SOEM_Port/osal.c`：SOEM 的裸机适配层。时间基准用 DWT 周期计数器，`sleep` 是忙等。
- `Makefile`：编译。`make` 出 `build/f407-eth-test.hex`。

## 五、上手操作
1. `make` 编译 → ST-Link 烧 `build/f407-eth-test.hex`。
2. picocom / 网页连 micro-USB 虚拟串口（115200）看日志、发命令。
3. **每次开机先 `h` 归零**，再 `g0`~`g4` 跑模式，否则从任意位置直接上升易超程报警。
